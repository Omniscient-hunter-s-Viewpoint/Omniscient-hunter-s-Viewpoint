import React from 'react';
import '../scss/detailModal.scss'
const DetailModal = () => {
    return (
        <section id="detailModal" style={{backgroundImage:'url(/img/gma.png)'}}>

        </section>
    );
};

export default DetailModal;